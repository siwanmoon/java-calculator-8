package calculator.model;

import java.util.ArrayList;
import calculator.common.Constants;

public class CheckUnexpectedChar {
    public static void checkChar (String inputString, ArrayList<String> separator) {
        if (separator.size() > Constants.DEFAULT_SEPARATOR.size()) { //커스텀 구분자가 존재할시 //(커스텀 구분자)\n 부분 문자열에서 제거

        }
    }
}
